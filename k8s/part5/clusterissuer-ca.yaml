apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sre-lab-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: sre-lab-ca
  secretName: sre-lab-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: sre-lab-ca-issuer
spec:
  ca:
    secretName: sre-lab-ca-secret
